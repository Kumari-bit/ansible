- name: nginx install and run
  hosts: frontend
  become: yes # sudo access
  tasks:
  - name: Install nginx
    ansible.builtin.package:
      name: nginx
      state: present
  - name: start nginx
    ansible.builtin.service:
      name: startnginx
      state: start
      enabled: yes
      