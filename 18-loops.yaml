- name: install packages
  hosts: frontend
  tasks:
   - name: install packages
     ansible.builtin.packages:
      msg: "{{item}}"
      state: installed
     loops:
     - mysql
     - nginx
     - zip
    